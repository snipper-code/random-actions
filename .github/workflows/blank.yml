name: Tag creation check

on:
  create:
    branches:
      - master
    tags:
      - v1

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: npm install, build, and test
      run: |
        echo 'tag creation runs '
      env:
        CI: true
